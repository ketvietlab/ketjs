// No extensions to register — unlike `ket-table-view.tsx`, `search-filter` has
// no escape hatch that needs a ketsuite-side registration point. This file
// exists only so the browser bundle is built from ketsuite's own client
// directory, exactly like every other island's entry.
export { searchFilter } from '@ketvietlab/design-system'
