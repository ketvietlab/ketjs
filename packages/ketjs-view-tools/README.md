# @ketvietlab/ketjs-view-tools

Static-site tooling for `@ketvietlab/ketjs-view`. It renders marker-free page HTML, bundles CSS and JavaScript, and preserves hydration markers only inside explicit islands.

Use `npm create @ketvietlab/view@latest my-site` to start a project, then run `npm run dev`.
