# __NAME__

A static HTML site built with Ket view.

- `npm run dev` starts the local development server.
- `npm run build` writes deployable HTML, CSS, and JavaScript to `dist`.
- `npm run preview` serves the built output.
- `npm run check` type-checks and validates pages and island bundles.
