# Dependencies
node_modules/

# Build and type artifacts
.build/
.types/
.ket-build.lock/
.ket-build-stage-*/
packages/*/dist/
# esbuild output for the Flow collaborative editor — regenerated by
# tools/build-flow-client.mjs on every build, never hand-edited. The rest of
# client/ (flow.css) is hand-written and stays tracked, same as every other
# _backend module's client/ directory.
packages/ketsuite/src/modules/flow_backend/client/editor.mjs
packages/ketsuite/src/modules/flow_backend/client/editor.mjs.map
coverage/
.release/
*.tsbuildinfo

# Ket runtime state
.ket/
*.db
*.db-journal
*.db-shm
*.db-wal
*.sqlite
*.sqlite-journal

# Local configuration and logs
.env
.env.*
!.env.example
*.log
npm-debug.log*

# Editors and operating systems
.idea/
.vscode/
*.swp
.DS_Store
